server {
  listen 80;
  listen [::]:80;

  root /home/static/InDots;
  index index.html index.htm;

  server_name sandbox.indots.getmore.mx www.sandbox.indots.getmore.mx;

  location / {
      try_files $uri $uri/ =404;
  }
}